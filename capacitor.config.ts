import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amorii7.cameraShareApp',
  appName: 'mobiledevelopmentapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
